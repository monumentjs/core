{
  "private": true,
  "version": "0.9.0",
  "author": "Alex Chugaev <achugaev93@gmail.com>",
  "license": "MIT",
  "homepage": "https://monumentjs.github.io",
  "repository": "https://github.com/monumentjs/core.git",
  "engines": {
    "node": ">=8"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "prepublishOnly": "npm run lint && npm run compile && lerna run test",
    "postinstall": "npm run bootstrap",
    "lint": "tslint --format stylish --project .",
    "fixstyle": "tslint --fix --format stylish --project .",
    "compile": "tsc",
    "test": "lerna run test",
    "build": "npm run lint && npm run compile && npm install && lerna run test",
    "clean": "gulp clean",
    "recompile": "gulp clean && tsc"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^2.11.0",
    "tslint": "^5.11.0",
    "typescript": "^3.0.1",
    "gulp": "^3.9.1",
    "gulp-clean": "^0.4.0"
  }
}
