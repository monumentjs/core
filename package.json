{
  "private": true,
  "author": "Alex Chugaev <achugaev93@gmail.com>",
  "license": "MIT",
  "repository": "https://github.com/monumentjs/core.git",
  "engines": {
    "node": ">=6"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "prepublishOnly": "npm run lint && npm run compile && lerna run test",
    "postinstall": "npm run bootstrap",
    "lint": "tslint --fix --format stylish --project .",
    "compile": "tsc",
    "test": "lerna run test"
  },
  "dependencies": {
    "reflect-metadata": "^0.1.12",
    "deep-equal": "^1.0.1"
  },
  "devDependencies": {
    "@types/node": "^10.0.0",
    "@types/deep-equal": "^1.0.1",
    "coveralls": "^3.0.0",
    "lerna": "^2.11.0",
    "tslint": "^5.9.1",
    "typescript": "^2.8.3"
  }
}
