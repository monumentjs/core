import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const MIDDLE_EUROPEAN_TIME = new TimeZone(TimeZoneOffset.positive(1), 'MET', 'Middle European Time');
