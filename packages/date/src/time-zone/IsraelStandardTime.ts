import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const ISRAEL_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(2), 'IST', 'Israel Standard Time');
