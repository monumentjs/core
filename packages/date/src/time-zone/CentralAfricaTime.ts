import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const CENTRAL_AFRICA_TIME = new TimeZone(TimeZoneOffset.positive(2), 'CAT', 'Central Africa Time');
