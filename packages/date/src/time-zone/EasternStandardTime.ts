import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const EASTERN_STANDARD_TIME = new TimeZone(TimeZoneOffset.negative(5), 'EST', 'Eastern Standard Time');
