import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const THAILAND_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(7), 'THA', 'Thailand Standard Time');
