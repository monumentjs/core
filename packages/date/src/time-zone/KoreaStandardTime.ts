import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const KOREA_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(9), 'KST', 'Korea Standard Time');
