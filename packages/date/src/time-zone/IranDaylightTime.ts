import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const IRAN_DAYLIGHT_TIME = new TimeZone(TimeZoneOffset.positive(4, 30), 'IRDT', 'Iran Daylight Time');
