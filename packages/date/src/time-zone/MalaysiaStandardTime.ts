import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const MALAYSIA_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(8), 'MST', 'Malaysia Standard Time');
