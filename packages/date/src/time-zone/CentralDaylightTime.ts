import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const CENTRAL_DAYLIGHT_TIME = new TimeZone(TimeZoneOffset.negative(5), 'CDT', 'Central Daylight Time');
