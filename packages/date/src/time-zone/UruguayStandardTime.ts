import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const URUGUAY_STANDARD_TIME = new TimeZone(TimeZoneOffset.negative(3), 'UYT', 'Uruguay Standard Time');
