import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const VOSTOK_STATION_TIME = new TimeZone(TimeZoneOffset.positive(6), 'VOST', 'Vostok Station Time');
