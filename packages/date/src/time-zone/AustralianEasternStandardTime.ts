import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const AUSTRALIAN_EASTERN_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(10), 'AEST', 'Australian Eastern Standard Time');
