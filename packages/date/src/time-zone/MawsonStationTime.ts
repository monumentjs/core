import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const MAWSON_STATION_TIME = new TimeZone(TimeZoneOffset.positive(5), 'MAWT', 'Mawson Station Time');
