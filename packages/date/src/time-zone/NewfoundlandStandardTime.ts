import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const NEWFOUNDLAND_STANDARD_TIME = new TimeZone(TimeZoneOffset.negative(3, 30), 'NST', 'Newfoundland Standard Time');
