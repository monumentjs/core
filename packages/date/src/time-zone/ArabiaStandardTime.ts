import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const ARABIA_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(3), 'AST', 'Arabia Standard Time');
