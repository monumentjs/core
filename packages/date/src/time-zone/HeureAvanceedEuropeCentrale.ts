import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const HEURE_AVANCEED_EUROPE_CENTRALE = new TimeZone(TimeZoneOffset.positive(2), 'HAEC', 'Heure Avanceed Europe Centrale');
