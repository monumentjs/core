import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const LORD_HOWE_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(10, 30), 'LHST', 'Lord Howe Standard Time');
