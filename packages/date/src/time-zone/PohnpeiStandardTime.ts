import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const POHNPEI_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(11), 'PONT', 'Pohnpei Standard Time');
