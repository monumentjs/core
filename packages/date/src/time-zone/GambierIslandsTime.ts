import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const GAMBIER_ISLANDS_TIME = new TimeZone(TimeZoneOffset.negative(9), 'GAMT', 'Gambier Islands Time');
