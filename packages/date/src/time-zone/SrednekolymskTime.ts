import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const SREDNEKOLYMSK_TIME = new TimeZone(TimeZoneOffset.positive(11), 'SRET', 'Srednekolymsk Time');
