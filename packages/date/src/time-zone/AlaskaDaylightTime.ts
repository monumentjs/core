import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const ALASKA_DAYLIGHT_TIME = new TimeZone(TimeZoneOffset.negative(8), 'AKDT', 'Alaska Daylight Time');
