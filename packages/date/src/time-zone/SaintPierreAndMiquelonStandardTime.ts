import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const SAINT_PIERRE_AND_MIQUELON_STANDARD_TIME = new TimeZone(TimeZoneOffset.negative(3), 'PMST', 'Saint Pierre And Miquelon Standard Time');
