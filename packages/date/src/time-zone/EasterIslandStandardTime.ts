import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const EASTER_ISLAND_STANDARD_TIME = new TimeZone(TimeZoneOffset.negative(6), 'EAST', 'Easter Island Standard Time');
