import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const CHILE_STANDARD_TIME = new TimeZone(TimeZoneOffset.negative(4), 'CLT', 'Chile Standard Time');
