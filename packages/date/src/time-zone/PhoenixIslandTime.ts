import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const PHOENIX_ISLAND_TIME = new TimeZone(TimeZoneOffset.positive(13), 'PHOT', 'Phoenix Island Time');
