import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const CHOIBALSAN_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(8), 'CHOT', 'Choibalsan Standard Time');
