import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const CENTRAL_WESTERN_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(8, 45), 'CWST', 'Central Western Standard Time');
