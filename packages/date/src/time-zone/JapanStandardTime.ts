import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const JAPAN_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(9), 'JST', 'Japan Standard Time');
