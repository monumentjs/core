import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const HAWAII_ALEUTIAN_STANDARD_TIME = new TimeZone(TimeZoneOffset.negative(10), 'HST', 'Hawaii Aleutian Standard Time');
