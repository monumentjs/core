import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const COORDINATED_UNIVERSAL_TIME = new TimeZone(TimeZoneOffset.positive(0), 'UTC', 'Coordinated Universal Time');
