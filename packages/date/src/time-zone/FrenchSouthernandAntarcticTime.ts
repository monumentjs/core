import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const FRENCH_SOUTHERN_AND_ANTARCTIC_TIME = new TimeZone(TimeZoneOffset.positive(5), 'TFT', 'French Southern And Antarctic Time');
