import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const SINGAPORE_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(8), 'SST', 'Singapore Standard Time');
