import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const MACQUARIE_ISLAND_STATION_TIME = new TimeZone(TimeZoneOffset.positive(11), 'MIST', 'Macquarie Island Station Time');
