import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const IRISH_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(1), 'IST', 'Irish Standard Time');
