import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const AUSTRALIAN_WESTERN_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(8), 'AWST', 'Australian Western Standard Time');
