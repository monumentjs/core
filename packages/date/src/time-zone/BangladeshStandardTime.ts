import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const BANGLADESH_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(6), 'BST', 'Bangladesh Standard Time');
