import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const MARSHALL_ISLANDS_TIME = new TimeZone(TimeZoneOffset.positive(12), 'MHT', 'Marshall Islands Time');
