import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const CENTRAL_INDONESIA_TIME = new TimeZone(TimeZoneOffset.positive(8), 'CIT', 'Central Indonesia Time');
