import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const SOUTH_AFRICAN_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(2), 'SAST', 'South African Standard Time');
