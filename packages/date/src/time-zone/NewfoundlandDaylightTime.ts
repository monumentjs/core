import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const NEWFOUNDLAND_DAYLIGHT_TIME = new TimeZone(TimeZoneOffset.negative(2, 30), 'NDT', 'Newfoundland Daylight Time');
