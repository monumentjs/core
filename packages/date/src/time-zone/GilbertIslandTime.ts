import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const GILBERT_ISLAND_TIME = new TimeZone(TimeZoneOffset.positive(12), 'GILT', 'Gilbert Island Time');
