import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const CHOIBALSAN_SUMMER_TIME = new TimeZone(TimeZoneOffset.positive(9), 'CHOST', 'Choibalsan Summer Time');
