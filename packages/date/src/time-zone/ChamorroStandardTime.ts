import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const CHAMORRO_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(10), 'CHST', 'Chamorro Standard Time');
