import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const INDIAN_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(5, 30), 'IST', 'Indian Standard Time');
