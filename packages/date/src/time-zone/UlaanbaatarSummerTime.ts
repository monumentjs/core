import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const ULAANBAATAR_SUMMER_TIME = new TimeZone(TimeZoneOffset.positive(9), 'ULAST', 'Ulaanbaatar Summer Time');
