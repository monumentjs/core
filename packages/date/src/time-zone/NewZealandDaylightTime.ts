import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const NEW_ZEALAND_DAYLIGHT_TIME = new TimeZone(TimeZoneOffset.positive(13), 'NZDT', 'New Zealand Daylight Time');
