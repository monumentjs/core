import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const BOUGAINVILLE_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(11), 'BST', 'Bougainville Standard Time');
