import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const NORFOLK_ISLAND_TIME = new TimeZone(TimeZoneOffset.positive(11), 'NFT', 'Norfolk Island Time');
