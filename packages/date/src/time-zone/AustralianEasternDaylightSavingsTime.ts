import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const AUSTRALIAN_EASTERN_DAYLIGHT_SAVINGS_TIME = new TimeZone(TimeZoneOffset.positive(11), 'AEDT', 'Australian Eastern Daylight Savings Time');
