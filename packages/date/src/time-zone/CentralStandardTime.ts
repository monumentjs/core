import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const CENTRAL_STANDARD_TIME = new TimeZone(TimeZoneOffset.negative(6), 'CST', 'Central Standard Time');
