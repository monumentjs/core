import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const PACIFIC_DAYLIGHT_TIME = new TimeZone(TimeZoneOffset.negative(7), 'PDT', 'Pacific Daylight Time');
