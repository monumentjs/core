import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const PAKISTAN_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(5), 'PKT', 'Pakistan Standard Time');
