import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const ULAANBAATAR_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(8), 'ULAT', 'Ulaanbaatar Standard Time');
