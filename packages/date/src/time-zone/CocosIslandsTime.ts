import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const COCOS_ISLANDS_TIME = new TimeZone(TimeZoneOffset.positive(6, 30), 'CCT', 'Cocos Islands Time');
