import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const WESTERN_EUROPEAN_SUMMER_TIME = new TimeZone(TimeZoneOffset.positive(1), 'WEST', 'Western European Summer Time');
