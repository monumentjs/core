import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const EASTERN_INDONESIAN_TIME = new TimeZone(TimeZoneOffset.positive(9), 'EIT', 'Eastern Indonesian Time');
