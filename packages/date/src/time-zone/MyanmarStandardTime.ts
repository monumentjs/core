import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const MYANMAR_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(6, 30), 'MMT', 'Myanmar Standard Time');
