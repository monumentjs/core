import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const CENTRAL_EUROPEAN_SUMMER_TIME = new TimeZone(TimeZoneOffset.positive(2), 'CEST', 'Central European Summer Time');
