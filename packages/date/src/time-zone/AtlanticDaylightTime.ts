import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const ATLANTIC_DAYLIGHT_TIME = new TimeZone(TimeZoneOffset.negative(3), 'ADT', 'Atlantic Daylight Time');
