import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const MIDDLE_EUROPEAN_SUMMER_TIME = new TimeZone(TimeZoneOffset.positive(2), 'MEST', 'Middle European Summer Time');
