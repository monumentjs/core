import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const SRI_LANKA_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(5, 30), 'SLST', 'Sri Lanka Standard Time');
