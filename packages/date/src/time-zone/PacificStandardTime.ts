import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const PACIFIC_STANDARD_TIME = new TimeZone(TimeZoneOffset.negative(8), 'PST', 'Pacific Standard Time');
