import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const SOLOMON_ISLANDS_TIME = new TimeZone(TimeZoneOffset.positive(11), 'SBT', 'Solomon Islands Time');
