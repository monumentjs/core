import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const FURTHER_EASTERN_EUROPEAN_TIME = new TimeZone(TimeZoneOffset.positive(3), 'FET', 'Further Eastern European Time');
