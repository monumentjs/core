import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const CHATHAM_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(12, 45), 'CHAST', 'Chatham Standard Time');
