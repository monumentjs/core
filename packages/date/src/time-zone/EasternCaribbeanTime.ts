import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const EASTERN_CARIBBEAN_TIME = new TimeZone(TimeZoneOffset.negative(4), 'ECT', 'Eastern Caribbean Time');
