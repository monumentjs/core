import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const ISRAEL_DAYLIGHT_TIME = new TimeZone(TimeZoneOffset.positive(3), 'IDT', 'Israel Daylight Time');
