import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const WESTERN_INDONESIAN_TIME = new TimeZone(TimeZoneOffset.positive(7), 'WIT', 'Western Indonesian Time');
