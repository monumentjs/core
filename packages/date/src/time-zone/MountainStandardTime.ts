import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const MOUNTAIN_STANDARD_TIME = new TimeZone(TimeZoneOffset.negative(7), 'MST', 'Mountain Standard Time');
