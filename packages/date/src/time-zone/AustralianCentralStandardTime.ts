import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const AUSTRALIAN_CENTRAL_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(9, 30), 'ACST', 'Australian Central Standard Time');
