import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const LORD_HOWE_SUMMER_TIME = new TimeZone(TimeZoneOffset.positive(11), 'LHST', 'Lord Howe Summer Time');
