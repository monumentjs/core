import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const AZORES_SUMMER_TIME = new TimeZone(TimeZoneOffset.positive(0), 'AZOST', 'Azores Summer Time');
