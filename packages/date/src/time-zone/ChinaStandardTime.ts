import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const CHINA_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(8), 'CST', 'China Standard Time');
