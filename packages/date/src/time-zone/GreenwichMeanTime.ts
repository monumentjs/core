import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const GREENWICH_MEAN_TIME = new TimeZone(TimeZoneOffset.positive(0), 'GMT', 'Greenwich Mean Time');
