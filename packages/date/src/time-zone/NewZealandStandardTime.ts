import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const NEW_ZEALAND_STANDARD_TIME = new TimeZone(TimeZoneOffset.positive(12), 'NZST', 'New Zealand Standard Time');
