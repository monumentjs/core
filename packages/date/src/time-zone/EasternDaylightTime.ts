import { TimeZone } from '../TimeZone';
import { TimeZoneOffset } from '../TimeZoneOffset';

export const EASTERN_DAYLIGHT_TIME = new TimeZone(TimeZoneOffset.negative(4), 'EDT', 'Eastern Daylight Time');
