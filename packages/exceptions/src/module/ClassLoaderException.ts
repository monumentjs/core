import { RuntimeException } from '../base/RuntimeException';

/**
 * @todo at next step this exception need to be split to ClassNotFoundException and FunctionNotFoundException
 * @author Alex Chugaev
 * @since 0.0.1
 */
export class ClassLoaderException extends RuntimeException {}
