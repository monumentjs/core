

export interface IFilter {
    /**
     * Gets or sets a value indicating whether more than one instance
     * of the indicated attribute can be specified for a single program element.
     */
    allowMultiple: boolean;
}
